# Imported directly, not just via FastAPI. Relying on a transitive dependency
# is what left greenlet uninstalled on Python 3.13 and broke every DB call.
pydantic>=2.7
# 0.111.0 pinned an old Starlette with a real CVE: on Windows, StaticFiles did
# not confine a resolved path, so a UNC path (\\host\share) made the server
# open an outbound SMB connection -- a known vector for leaking the machine's
# NTLM hash to an attacker-controlled listener. This app's own SPA route has
# its own containment check (ensure_within_directory) and was never affected,
# but the raw /assets mount in main.py uses Starlette's StaticFiles directly.
starlette>=1.0.1

fastapi==0.141.1
uvicorn==0.29.0
# Not imported anywhere -- Atlas reads .env with its own loader in
# app/utils/env_utils.py. It arrives transitively (pydantic-settings, uvicorn),
# and pinning it here only served to hold it at a version with a symlink-
# following bug in set_key/unset_key. Left to resolve forward.
sqlalchemy[asyncio]==2.0.36
alembic==1.13.1
aiosqlite==0.20.0
httpx>=0.27.0
cryptography>=42.0.0
# 0.0.9 carried several denial-of-service parsing bugs (unbounded part-header
# counts, a negative Content-Length turning a bounded read into read-until-EOF).
python-multipart>=0.0.18
pymupdf>=1.24.0
python-docx>=1.1.0
chardet>=5.2.0
Pillow>=10.3.0
pytesseract>=0.3.10
opencv-python-headless>=4.9.0
chromadb>=0.5.0
tiktoken>=0.7.0

# Local embeddings, so a fresh install can index and search documents before
# any API key exists. Runs a small ONNX model on the CPU -- no account, and no
# network after it fetches the model (~130MB) on first use.
fastembed>=0.8.0

apscheduler>=3.10.0
# Imported directly by the knowledge graph and OCR pipelines. Both were
# previously only present transitively, so a clean install could not boot.
networkx>=3.2
numpy>=1.26
