[project]
name = "atlas-backend"
version = "0.2.0"
license = { text = "MIT" }
description = "Atlas local-first backend"
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["app/tests"]
pythonpath = ["."]
asyncio_mode = "auto"

[tool.ruff]
line-length = 100
target-version = "py311"

